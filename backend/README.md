# Backend placeholder

The backend is intentionally not implemented in the local UI phase.

The next phase will use Python AWS Lambda, API Gateway, private Amazon S3 storage, and DynamoDB. See the documents in `/docs` for the agreed contract and implementation order.
